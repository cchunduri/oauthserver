package com.chaiapps.oauthserver.utils

class SecurityConstants {
    companion object {
        const val AUTHORITIES_KEY = "auth"
        const val INVALID_JWT_TOKEN = "Invalid JWT token."

        const val AUTHORIZATION_HEADER = "Authorization"
    }
}